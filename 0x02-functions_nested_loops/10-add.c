#include "main.h"

/**
 * add - This function adds two integers
 * @y: the first value
 * @z: the 2nd value
 * Return: Returns the value of the addition
 */

int add(int y, int z)
{
	return (y + z);
}
